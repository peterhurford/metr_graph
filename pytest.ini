[pytest]
# The 75 AppTest cases in test_integration.py are independent and each costs
# real wall time, so run them in parallel. Pass `-n0` to go serial — needed for
# --pdb, and for live output from a single test. See conftest.py for the other
# two speedups (shared bytecode cache, smaller Monte Carlo).
addopts = -n auto
